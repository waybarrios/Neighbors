<h1 class="small-9 small-centered large-uncentered columns">Listing comments</h1>

<table class="small-10 small-push-2 columns">
  <tr>
    <th>Content</th>
    <th>User</th>
    <th>Event</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @comments.each do |comment| %>
  <tr class="small-10 small-push-2 columns">
    <td><%= comment.content %></td>
    <td><%=  %></td>
    <td><%=  %></td>
    <td><%= link_to 'Show', comment %></td>
    <td><%= link_to 'Edit', edit_comment_path(comment) %></td>
    <td><%= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Comment', new_comment_path, class: "button tiny radius medium-5 small-5 small-push-7 medium-push-3 columns success" %>
