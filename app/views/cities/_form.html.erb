<%= form_for(@city) do |f| %>
  <% if @city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% @city.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="small-12 small-centered medium-9 medium-centered large-9 large-centered columns">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
</div>
 <div class="small-12 small-centered medium-9 medium-centered large-9 large-centered columns">
    <%= f.label :country  %><br />
    <%= f.select :country_id, options_for_select(Country.all.map {|
      country| [country.name, country.id]}) %>
</div>

  <div class="actions">
    <%= f.submit nil, class: "button radius medium-5 small-12 medium-push-3 columns success" %>
  </div>
<% end %>
